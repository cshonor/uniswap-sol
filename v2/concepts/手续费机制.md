# Uniswap V2 手续费机制详解

本文档详细说明 Uniswap V2 中的手续费机制，包括手续费的计算、分配和累积方式。

## 📋 概述

Uniswap V2 对每笔交易收取 **0.3%** 的手续费，这些手续费会累积在流动性池中，分配给流动性提供者（LP）。

---

## 💰 手续费率

### 标准手续费率

- **交易手续费**：**0.3%**（每笔交换）
- **固定费率**：所有交易对使用相同的手续费率
- **无协议费用**：所有手续费都分配给流动性提供者

### 手续费计算

**重要原则**：手续费是从**输入金额**（用户支付的代币）中扣除的，而不是从输出金额中扣除。

手续费在交换时自动扣除，通过调整恒定乘积公式实现：

**无手续费的公式：**
```
(x + Δx) * (y - Δy) = x * y
```

**有手续费的公式：**
```
(x + Δx * 0.997) * (y - Δy) = x * y
```

其中：
- `0.997 = 1 - 0.003`（扣除 0.3% 手续费）
- `Δx * 0.997`：实际进入池子的输入数量（扣除手续费后）
- **手续费金额** = `Δx * 0.003`（留在池子中，分配给 LP）

**示例**：
- 用户输入：1000 DAI
- 手续费：1000 × 0.003 = 3 DAI（留在池子中）
- 实际进入池子：1000 × 0.997 = 997 DAI

---

## 🔢 手续费计算方式

### 实际实现

在 Uniswap V2 中，手续费通过调整输入数量来实现：

```solidity
function getAmountOut(
    uint256 amountIn,      // 输入的代币数量（用户要交换的数量）
    uint256 reserveIn,     // 输入代币在池子中的储备量
    uint256 reserveOut     // 输出代币在池子中的储备量
) public pure returns (uint256 amountOut) {
    require(amountIn > 0, "Insufficient input amount");
    require(reserveIn > 0 && reserveOut > 0, "Insufficient liquidity");
    
    // 步骤 1: 计算手续费调整后的输入数量（整数运算优化）
    // 手续费率 f = 0.003，手续费因子 (1-f) = 0.997
    // 注意：这里 amountIn * 997 并不是最终值，而是为了整数运算的中间值
    // 实际公式：amountOut = (amountIn * 997 * reserveOut) / (reserveIn * 1000 + amountIn * 997)
    // 等价于：amountOut = ((amountIn * 0.997) * reserveOut) / (reserveIn + amountIn * 0.997)
    // 为了整数运算，分子分母同时乘以 1000，得到上面的形式
    uint256 amountInWithFee = amountIn * 997;  // 注意：这是 amountIn * 997，不是 amountIn * 0.997
    
    // 步骤 2: 计算分子
    // 分子 = amountIn * 997 * reserveOut
    // 等价于：(amountIn * 0.997) * reserveOut * 1000
    uint256 numerator = amountInWithFee * reserveOut;
    
    // 步骤 3: 计算分母
    // 分母 = reserveIn * 1000 + amountIn * 997
    // 等价于：(reserveIn + amountIn * 0.997) * 1000
    // 注意：分母中的 1000 是为了与分子的 1000 抵消，保持整数运算精度
    uint256 denominator = (reserveIn * 1000) + amountInWithFee;
    
    // 步骤 4: 计算输出数量
    // amountOut = numerator / denominator
    // 等价于：dy = ((1-f) * dx * y₀) / (x₀ + (1-f) * dx)
    amountOut = numerator / denominator;
}
```

**计算示例：**

**数据来源说明：**

在实际使用中，这些数据来自不同的来源：

1. **`amountIn`**：用户输入的参数
   - 用户想要交换的代币数量
   - 例如：用户想要用 1000 DAI 换 USDT

2. **`reserveIn` 和 `reserveOut`**：从链上查询的池子储备量
   - 通过调用 Pair 合约的 `getReserves()` 函数获取
   - 这些是池子当前的代币储备量，会实时变化

**示例数据（仅用于演示计算过程）：**

假设：
- `amountIn = 1000` DAI（用户要交换的数量）
- `reserveIn = 10000` DAI（从链上查询的池子 DAI 储备）
- `reserveOut = 20000` USDT（从链上查询的池子 USDT 储备）

**步骤 1：计算 amountInWithFee**
```
amountInWithFee = 1000 * 997 = 997,000
```
注意：这不是 `1000 * 0.997 = 997`，而是 `1000 * 997 = 997,000`

**步骤 2：计算分子**
```
numerator = 997,000 * 20000 = 19,940,000,000
```

**步骤 3：计算分母**
```
denominator = (10000 * 1000) + 997,000 = 10,000,000 + 997,000 = 10,997,000
```

**步骤 4：计算输出**
```
amountOut = 19,940,000,000 / 10,997,000 ≈ 1,813.6 USDT
```

**验证（使用小数运算）：**
```
实际进入池子 = 1000 * 0.997 = 997 DAI
amountOut = (997 * 20000) / (10000 + 997) = 19,940,000 / 10,997 ≈ 1,813.6 USDT
```

结果一致！整数运算通过分子分母同时乘以 1000 来避免浮点数。

**如何在实际代码中获取储备量：**

```solidity
// 1. 获取 Pair 合约地址（通过 Factory）
address pairAddress = factory.getPair(tokenA, tokenB);

// 2. 调用 Pair 合约的 getReserves() 函数
IUniswapV2Pair pair = IUniswapV2Pair(pairAddress);
(uint112 reserve0, uint112 reserve1, uint32 blockTimestampLast) = pair.getReserves();

// 3. 根据代币顺序确定 reserveIn 和 reserveOut
// 注意：需要确认 token0 和 token1 的对应关系
address token0 = pair.token0();
address token1 = pair.token1();

uint256 reserveIn;
uint256 reserveOut;

if (tokenIn == token0) {
    reserveIn = reserve0;
    reserveOut = reserve1;
} else {
    reserveIn = reserve1;
    reserveOut = reserve0;
}

// 4. 调用 getAmountOut 计算输出
uint256 amountOut = getAmountOut(amountIn, reserveIn, reserveOut);
```

**注意事项：**
- `getReserves()` 返回的是调用时的储备量，可能不是最新的（取决于最后更新时间）
- 在实际交易前，应该使用最新的储备量进行计算
- Router 合约会自动处理这些细节

### 反向计算：`getAmountIn`（已知输出，求输入）

`getAmountIn` 是 `getAmountOut` 的反向计算，用于 `swapTokensForExactTokens` 函数。

**函数签名：**
```solidity
function getAmountIn(
    uint256 amountOut,    // 输出的代币数量（用户想要得到的数量）
    uint256 reserveIn,    // 输入代币在池子中的储备量
    uint256 reserveOut     // 输出代币在池子中的储备量
) public pure returns (uint256 amountIn) {
    require(amountOut > 0, "Insufficient output amount");
    require(reserveIn > 0 && reserveOut > 0, "Insufficient liquidity");
    
    // 从公式 dy = ((1-f) * dx * y₀) / (x₀ + (1-f) * dx)
    // 推导：dx = (dy * x₀) / ((1-f) * (y₀ - dy))
    // 使用整数运算：amountIn = (amountOut * reserveIn * 1000) / ((reserveOut - amountOut) * 997)
    uint256 numerator = amountOut * reserveIn * 1000;
    uint256 denominator = (reserveOut - amountOut) * 997;
    amountIn = (numerator / denominator) + 1;  // +1 是为了避免舍入误差
}
```

**公式推导：**

从正向公式推导反向公式：

```
已知：dy = ((1-f) * dx * y₀) / (x₀ + (1-f) * dx)

其中 f = 0.003, (1-f) = 0.997

展开：dy * (x₀ + (1-f) * dx) = (1-f) * dx * y₀
     dy * x₀ + dy * (1-f) * dx = (1-f) * dx * y₀
     dy * x₀ = (1-f) * dx * (y₀ - dy)
     
因此：dx = (dy * x₀) / ((1-f) * (y₀ - dy))
```

**代码实现（整数运算）：**

```solidity
function getAmountIn(
    uint256 amountOut,
    uint256 reserveIn,
    uint256 reserveOut
) public pure returns (uint256 amountIn) {
    require(amountOut > 0, "Insufficient output amount");
    require(reserveIn > 0 && reserveOut > 0, "Insufficient liquidity");
    
    // 步骤 1: 计算分子
    // 分子 = amountOut * reserveIn * 1000
    // 等价于：dy * x₀ * 1000
    uint256 numerator = amountOut * reserveIn * 1000;
    
    // 步骤 2: 计算分母
    // 分母 = (reserveOut - amountOut) * 997
    // 等价于：(y₀ - dy) * (1-f) * 1000，其中 (1-f) = 0.997
    uint256 denominator = (reserveOut - amountOut) * 997;
    
    // 步骤 3: 计算输入数量
    // amountIn = numerator / denominator + 1
    // +1 是为了避免整数除法的舍入误差，确保有足够的输入
    amountIn = (numerator / denominator) + 1;
}
```

**计算示例：**

假设：
- `amountOut = 1000` USDT（用户想要得到的数量）
- `reserveIn = 10000` DAI（池子中的 DAI 储备）
- `reserveOut = 20000` USDT（池子中的 USDT 储备）

**步骤 1：计算分子**
```
numerator = 1000 * 10000 * 1000 = 10,000,000,000
```

**步骤 2：计算分母**
```
denominator = (20000 - 1000) * 997 = 19000 * 997 = 18,943,000
```

**步骤 3：计算输入**
```
amountIn = 10,000,000,000 / 18,943,000 + 1 ≈ 528 + 1 = 529 DAI
```

**验证（使用小数运算）：**
```
amountIn = (1000 * 10000) / (0.997 * (20000 - 1000))
         = 10,000,000 / (0.997 * 19000)
         = 10,000,000 / 18,943
         ≈ 527.8 DAI
```

结果一致！代码中的 `+1` 确保了有足够的输入来得到所需的输出。

### 多跳反向计算：`getAmountsIn`（多跳交换的反向计算）

`getAmountsIn` 用于计算多跳交换中，为了得到确定的输出数量，每一步需要的输入数量。

**函数签名：**
```solidity
function getAmountsIn(
    address factory,           // Factory 合约地址，用于获取交易对储备量
    uint256 amountOut,         // 最终输出的代币数量（用户想要得到的数量）
    address[] memory path      // 交换路径，如 [DAI, USDT, MKR]
) internal view returns (uint256[] memory amounts) {
    require(path.length >= 2, "UniswapV2Library: INVALID_PATH");
    amounts = new uint256[](path.length);
    
    // 步骤 1: 设置最终输出数量
    amounts[amounts.length - 1] = amountOut;
    
    // 步骤 2: 反向迭代计算每一步的输入
    // 从路径的最后一个代币开始，向前计算每一步需要的输入
    for (uint256 i = path.length - 1; i > 0; i--) {
        // 获取当前交易对的储备量
        (uint256 reserveIn, uint256 reserveOut) = getReserves(
            factory, 
            path[i - 1],  // 输入代币
            path[i]       // 输出代币
        );
        
        // 反向计算：已知输出，求输入
        // amounts[i] 是当前步骤的输出（也是下一步的输入）
        // amounts[i-1] 是当前步骤需要的输入
        amounts[i - 1] = getAmountIn(amounts[i], reserveIn, reserveOut);
    }
    
    return amounts;
}
```

**核心逻辑：**

1. **反向迭代**：从路径的最后一个代币开始，向前计算
2. **每一步计算**：使用 `getAmountIn` 计算当前步骤需要的输入
3. **链式计算**：每一步的输出是下一步的输入

**计算过程示例**（DAI → USDT → MKR）：

假设：
- `amountOut = 50` MKR（最终想要得到的数量）
- `path = [DAI, USDT, MKR]`
- USDT/MKR 池子：reserveIn = 20000 USDT, reserveOut = 1000 MKR
- DAI/USDT 池子：reserveIn = 10000 DAI, reserveOut = 20000 USDT

**步骤 1：初始化**
```
amounts = [0, 0, 50]  // 最后一个是输出数量
```

**步骤 2：第 2 跳反向计算（USDT → MKR）**
```
i = 2 (path.length - 1)
- 输出：amounts[2] = 50 MKR
- 储备：reserveIn = 20000 USDT, reserveOut = 1000 MKR
- 计算输入：amounts[1] = getAmountIn(50, 20000, 1000)
           = (50 * 20000 * 1000) / ((1000 - 50) * 997) + 1
           ≈ 1053 USDT
```

**步骤 3：第 1 跳反向计算（DAI → USDT）**
```
i = 1
- 输出：amounts[1] = 1053 USDT（上一步的输入）
- 储备：reserveIn = 10000 DAI, reserveOut = 20000 USDT
- 计算输入：amounts[0] = getAmountIn(1053, 10000, 20000)
           = (1053 * 10000 * 1000) / ((20000 - 1053) * 997) + 1
           ≈ 558 DAI
```

**最终结果：**
```
amounts = [558, 1053, 50]
含义：
- 需要 558 DAI 作为初始输入
- 第一步交换得到 1053 USDT
- 第二步交换得到 50 MKR（最终输出）
```

**关键点：**

1. **反向计算**：从最终输出开始，向前计算每一步的输入
2. **手续费累积**：每一步都会扣除 0.3% 手续费，所以多跳交换的手续费会累积
3. **链式依赖**：每一步的输出依赖于下一步的计算结果
4. **整数运算**：每一步都使用整数运算，避免浮点数误差

**公式推导：**

假设：
- `x₀`：当前池子中 token0 的储备量
- `y₀`：当前池子中 token1 的储备量
- `dx`：用户输入的 token0 数量（Swap Exact Tokens）
- `dy`：用户得到的 token1 数量
- `k`：恒定乘积

**步骤 1：无手续费情况下的恒定乘积公式**

```
(x₀ + dx) ⋅ (y₀ - dy) = k = x₀ ⋅ y₀
```

**步骤 2：展开左边**

```
(x₀ + dx) ⋅ (y₀ - dy) = x₀y₀ - x₀dy + dx ⋅ y₀ - dxdy
```

**步骤 3：保持恒定乘积**

```
x₀y₀ - x₀dy + dx ⋅ y₀ - dxdy = x₀y₀
```

**步骤 4：简化（忽略二阶项）**

在实际计算中，`dxdy` 是一个二阶小量，可以忽略不计：

```
x₀y₀ - x₀dy + dx ⋅ y₀ - dxdy ≈ x₀y₀ - x₀dy + dx ⋅ y₀ = x₀y₀
```

移项后得到：

```
- x₀dy + dx ⋅ y₀ = 0
dx ⋅ y₀ = x₀dy + dxdy ≈ x₀dy
```

进一步整理：

```
dx ⋅ y₀ = dy(x₀ + dx)
```

**步骤 4.1：无手续费时的输出公式**

```
dy = (dx ⋅ y₀) / (x₀ + dx)
```

**步骤 5：引入手续费（0.3%）**

**关键点**：手续费是从**输入金额**（dx）中扣除的。

设手续费率为 `f = 0.003`（0.3%），则手续费因子为 `(1-f) = 0.997`。

实际进入池子的数量是：
```
(1-f) ⋅ dx = 0.997 ⋅ dx
```

**方法一：直接替换**

在无手续费公式 `dy = (dx ⋅ y₀) / (x₀ + dx)` 中，将 `dx` 替换为 `(1-f) ⋅ dx`：

```
dy = ((1-f) ⋅ dx ⋅ y₀) / (x₀ + (1-f) ⋅ dx)
```

代入 `f = 0.003`：

```
dy = (0.997 ⋅ dx ⋅ y₀) / (x₀ + 0.997 ⋅ dx)
```

**方法二：从恒定乘积公式推导**

```
(x₀ + (1-f) ⋅ dx) ⋅ (y₀ - dy) = x₀ ⋅ y₀
```

展开并忽略二阶项：

```
x₀y₀ - x₀dy + (1-f) ⋅ dx ⋅ y₀ ≈ x₀y₀
```

简化：

```
(1-f) ⋅ dx ⋅ y₀ = dy(x₀ + (1-f) ⋅ dx)
```

**步骤 6：最终公式**

```
dy = ((1-f) ⋅ dx ⋅ y₀) / (x₀ + (1-f) ⋅ dx)
```

其中 `f = 0.003`，`(1-f) = 0.997`

**步骤 7：代码实现（整数运算）**

在实际代码实现中，为了避免浮点数，使用整数运算：
- `0.997` 表示为 `997 / 1000`
- `f = 0.003` 表示为 `3 / 1000`
- 公式变为：`dy = (dx * 997 * y₀) / (x₀ * 1000 + dx * 997)`

### 手续费金额

**手续费 = 输入数量 × 0.3%**

例如：
- 输入：1000 DAI
- 手续费：1000 × 0.003 = 3 DAI
- 实际进入池子：997 DAI

---

## 📊 手续费分配

### 分配给流动性提供者

所有手续费都累积在流动性池中，按 LP tokens 的比例分配给流动性提供者：

1. **手续费累积**：
   - 每次交换，手续费留在池子中
   - 增加池子的总价值

2. **按比例分配**：
   - 每个 LP 的份额 = `LP tokens / totalSupply`
   - 手续费收益 = `池子总价值增长 × 份额比例`

3. **提取方式**：
   - 移除流动性时，按比例获得累积的手续费
   - 手续费以两种代币的形式存在池子中

### 手续费累积机制：x 和 y 都会缓慢增长

**重要概念**：手续费会导致池子中的两种代币储备量（x 和 y）都缓慢增长。

**原理说明**：

当用户进行交换时：
- 用户输入：`dx` 数量的 token0
- 手续费：`f × dx = 0.003 × dx`（留在池子中）
- 实际进入池子：`(1-f) × dx = 0.997 × dx`

**手续费分配方式**：

手续费 `0.003 × dx` 会以两种代币的形式留在池子中：

```
输入 dx
  ↓
├─ 0.997 × dx → 进入池子，参与交换计算
└─ 0.003 × dx → 手续费，留在池子中
```

**为什么 x 和 y 都会增长？**

1. **单次交换**：
   - 如果用户用 token0 换 token1，手续费以 token0 的形式留在池子中
   - 这会导致 `x`（token0 储备量）增加
   - 同时，由于交换，`y`（token1 储备量）会减少

2. **多次交换累积**：
   - 随着时间推移，有用户用 token0 换 token1，也有用户用 token1 换 token0
   - 每次交换都会留下手续费
   - 手续费会以对应的输入代币形式累积在池子中
   - **结果**：`x` 和 `y` 都会缓慢增长

**示例说明**：

假设池子初始状态：
- `x = 1000` DAI
- `y = 2000` USDT

**第 1 次交换**：用户用 1000 DAI 换 USDT
- 手续费：3 DAI（留在池子中）
- 池子变为：`x = 1997` DAI, `y = 1500` USDT（假设）
- `x` 增加了（因为手续费是 DAI）

**第 2 次交换**：用户用 500 USDT 换 DAI
- 手续费：1.5 USDT（留在池子中）
- 池子变为：`x = 1800` DAI, `y = 2001.5` USDT（假设）
- `y` 增加了（因为手续费是 USDT）

**长期累积**：
- 经过多次双向交换，`x` 和 `y` 都会因为手续费累积而缓慢增长
- 这增加了池子的总价值，为 LP 提供收益

**关键理解**：
- ✅ **手续费会累积**：每次交换都会留下手续费
- ✅ **双向增长**：随着双向交易，两种代币的储备量都会增长
- ✅ **价值增加**：池子总价值持续增加，LP 受益

### 示例

**初始状态：**
- 池子：1000 DAI + 2000 USDT
- 总价值：4000 USDT
- LP tokens：1414

**执行交换：**
- 用户用 1000 DAI 换 USDT
- 手续费：3 DAI（留在池子中）
- 池子变为：1997 DAI + 1500 USDT（假设）
- 总价值：4000 + 3 = 4003 USDT

**LP 收益：**
- 如果 LP 持有 100 LP tokens（约 7%）
- 收益：3 DAI × 7% ≈ 0.21 DAI

---

## 🔄 手续费对价格的影响

### 价格滑点

手续费会导致实际价格与理论价格略有差异：

**理论价格（无手续费）：**
```
价格 = reserve1 / reserve0
```

**实际价格（有手续费）：**
```
实际价格 = (reserve1 - amountOut) / (reserve0 + amountIn)
```

### 手续费对输出的影响

手续费会略微减少输出数量：

**无手续费：**
```
amountOut = (amountIn * reserveOut) / (reserveIn + amountIn)
```

**有手续费：**
```
amountOut = (amountIn * 0.997 * reserveOut) / (reserveIn * 1.0 + amountIn * 0.997)
```

---

## 💡 手续费的重要性

### 1. 激励流动性提供

- 手续费是 LP 的主要收益来源
- 鼓励用户提供流动性

### 2. 防止套利

- 手续费增加了套利成本
- 保护池子免受频繁小额套利攻击

### 3. 协议可持续性

- 手续费确保协议长期运行
- 为流动性提供者提供持续收益

---

## 📈 手续费收益计算

### LP 收益率

**年化收益率（APY）计算：**

```
APY = (每日手续费 × 365) / 池子总价值 × 100%
```

**示例：**
- 池子总价值：$1,000,000
- 每日交易量：$100,000
- 每日手续费：$100,000 × 0.3% = $300
- 年化手续费：$300 × 365 = $109,500
- APY = $109,500 / $1,000,000 × 100% = 10.95%

### 影响因素

1. **交易量**：交易量越大，手续费越多
2. **池子规模**：池子越大，单个 LP 的份额越小
3. **价格波动**：价格波动大，交易量通常也大

---

## ⚠️ 注意事项

### 1. 手续费累积

- 手续费不会立即分配给 LP
- 只有在移除流动性时才能获得
- 手续费以两种代币的形式累积在池子中

### 2. 无常损失

- LP 需要承担无常损失风险
- 手续费收益需要覆盖无常损失才能盈利

### 3. Gas 成本

- 频繁添加/移除流动性会产生 Gas 成本
- 需要权衡手续费收益和 Gas 成本

---

## 🔗 相关文档

- [Pair（交易对）合约](../core/Pair（交易对）合约.md) - 了解交换的实现
- [添加/移除流动性](./添加移除流动性.md) - 了解如何获得手续费收益
- [无常损失](./无常损失.md) - 了解 LP 的风险

---

## 🎓 总结

Uniswap V2 的手续费机制：

1. **固定费率**：0.3% 的交易手续费
2. **自动扣除**：通过调整输入数量实现
3. **全部分配**：所有手续费分配给流动性提供者
4. **累积方式**：手续费留在池子中，移除流动性时获得
5. **激励作用**：鼓励用户提供流动性，维持协议运行

手续费是 Uniswap V2 经济模型的核心，平衡了交易者和流动性提供者的利益。

